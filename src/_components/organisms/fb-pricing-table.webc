<script webc:type="js">
  const plans = read('plans').filter(forObservabilityWorkshop).sort(priceDescending);
  webc.attributes.object.plans = plans;
  `<fb-pricing-cards :object="object"></fb-pricing-cards>`
</script>